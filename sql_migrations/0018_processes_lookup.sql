-- Add processes lookup for team feedback process dropdown
CREATE TABLE IF NOT EXISTS processes_lookup (
  process_id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name VARCHAR NOT NULL UNIQUE,
  is_active BOOLEAN NOT NULL DEFAULT TRUE
);

INSERT INTO processes_lookup (name, is_active)
SELECT 'Client Onboarding', TRUE
WHERE NOT EXISTS (
  SELECT 1 FROM processes_lookup WHERE lower(name) = lower('Client Onboarding')
);

INSERT INTO processes_lookup (name, is_active)
SELECT 'Job Start', TRUE
WHERE NOT EXISTS (
  SELECT 1 FROM processes_lookup WHERE lower(name) = lower('Job Start')
);

INSERT INTO processes_lookup (name, is_active)
SELECT 'Job Report', TRUE
WHERE NOT EXISTS (
  SELECT 1 FROM processes_lookup WHERE lower(name) = lower('Job Report')
);

INSERT INTO processes_lookup (name, is_active)
SELECT 'General', TRUE
WHERE NOT EXISTS (
  SELECT 1 FROM processes_lookup WHERE lower(name) = lower('General')
);
